describe("App", () => {
  it("should show 'HomePage' when rooms is empty", () => {});
  it("should show 'ChatPage' when have rooms", () => {});
  it("should request new room to socket when 'new' event is received", () => {});
  it("should exit a room when 'exit' event is received", () => {});
  it("should apply a message to socket when 'send:message' event is received", () => {});
  it("should call 'sendChatTyping' socket method when 'send:typing' event is received", () => {});
});

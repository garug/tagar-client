describe("ChatPage", () => {
  it("should recalculate number chat view when 'numberChat' change", () => {});
  it("should not permit decrement 'numberChat' when active chats is greather than chat view", () => {});
  it("should permit decrement 'numberChat' when active chats is lower than chat view", () => {});
  it("should permit increment 'numberChat'", () => {});
  it("should resolve total chat pages based on 'numberChat'", () => {});
});
